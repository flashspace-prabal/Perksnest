// Re-export from unified client for backwards compatibility
export { supabase as supabaseAuth, SUPABASE_URL_VALUE as SUPABASE_URL, SUPABASE_ANON_KEY_VALUE as SUPABASE_ANON_KEY } from './supabaseClient';
export { supabase as db } from './supabaseClient';
export { supabase as default } from './supabaseClient';
